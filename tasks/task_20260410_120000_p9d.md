---
id: task_20260410_120000_p9d
title: "Phase 9: エージェントダッシュボード実装"
status: done
approval: approved
created: 2026-04-10
---

# Phase 9: エージェントダッシュボード実装

## ゴール

各エージェントにダッシュボードタブを追加し、`dashboard.md` をマークダウンで表示・編集できるようにする。

## 仕様参照

- `docs/spec_phase9.md`
- `docs/mockup_phase9.html`

---

## 作業ステップ（TDD）

### Step 1: 仕様書更新
- [x] `docs/spec_phase9.md` のストレージパスを `{agent.path}/.kobito/dashboard.md` に修正する

### Step 2: APIテスト作成（RED）
- [x] `src/tests/test_dashboard.py` を作成する
  - `GET /api/agents/{id}/dashboard` — 内容を返す
  - `GET /api/agents/{id}/dashboard` — ファイル不在時は空文字を返す
  - `PUT /api/agents/{id}/dashboard` — 内容を保存する
  - `PUT /api/agents/{id}/dashboard` — `.kobito/` ディレクトリが存在しない場合は自動作成する
- [x] テストが失敗することを確認する

### Step 3: API実装（GREEN）
- [x] `src/server/routes/dashboard.py` を作成する
  - `GET /api/agents/{id}/dashboard`
  - `PUT /api/agents/{id}/dashboard`
- [x] `src/server/app.py` にルートを登録する
- [x] 全APIテストが通ることを確認する（6/6 PASS）

### Step 4: E2Eテスト作成（RED）
- [x] `src/tests/test_e2e_dashboard.py` を作成する（Playwright）
  - ダッシュボードタブが表示される
  - `dashboard.md` の内容がマークダウンレンダリングされる
  - 編集ボタンを押すとテキストエリアが開く
  - 保存するとダッシュボードに反映される
  - ファイルリンクをクリックするとファイルタブに遷移する
- [x] テストが失敗することを確認する

### Step 5: フロントエンド実装（GREEN）
- [x] タブ構成変更（ダッシュボードを先頭に追加）
- [x] マークダウンレンダリング実装
- [x] 表示モード実装（編集ボタン付き）
- [x] 編集モード実装（テキストエリア、保存/キャンセル）
- [x] ファイルリンクのクリックでファイルタブに遷移する処理
- [x] 全E2Eテストが通ることを確認する（10/10 PASS）
