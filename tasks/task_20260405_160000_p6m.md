---
title: "Phase 6 AI間通信（MCP ask_agent）実装"
agent: system
phase: done
created: 2026-04-05
---

## ゴール

エージェントが `ask_agent` ツールで他エージェントに質問・回答を得られる仕組みを構築する。
仕様書: `docs/spec_phase6.md`

## 作業ステップ

- [x] 依存パッケージ導入 — `mcp`, `httpx` のインストール
- [x] 内部API テスト作成 — `POST /api/internal/ask` の正常系・異常系（404、自己送信400、タイムアウト504）テストを書く
- [x] 内部API 実装 — `src/server/routes/internal.py`。テストが通るまで実装
- [x] ループ防止 テスト作成 — `call_chain` による循環検出・最大長制限のテストを書く
- [x] ループ防止 実装 — テストが通るまで実装
- [x] MCPサーバー テスト作成 — `ask_agent` ツール定義の存在確認、内部APIへのリクエスト構築テストを書く
- [x] MCPサーバー 実装 — `src/mcp_server/ask_agent.py`。テストが通るまで実装
- [x] CLIBridge修正 テスト作成 — `_build_command()` が `--mcp-config` を含むことのテストを書く
- [x] CLIBridge修正 実装 — MCP設定ファイル生成 + `_build_command()` 修正。テストが通るまで実装
- [x] セッションメタ拡張 テスト作成 — `initiated_by` フィールドの記録・取得テストを書く
- [x] セッションメタ拡張 実装 + UI軽微修正 — テストが通るまで実装、セッション一覧に発信元表示
- [x] 共通指示更新 — `assets/prompts/shared_instructions.md` に `ask_agent` の使い方を追記
- [x] E2Eテスト — Agent A → Agent B → 回答の統合テスト
