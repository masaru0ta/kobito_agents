# リファクタリング作業前評価レポート

**評価日**: 2026-04-04
**対象**: `src/server/` 全コンポーネント（13ファイル, 約1,460行）

## 評価基準

| 観点 | 定義 |
|------|------|
| **可読性** | コードを初めて読む開発者が意図を理解できるか。命名、構造、コメントの質 |
| **メンテナンス性** | 機能追加・変更時の影響範囲が限定的か。責務分離、DRY原則、依存関係の明確さ |
| **堅牢性** | 異常系への耐性。エラーハンドリング、型安全性、競合状態への配慮 |

---

## コンポーネント別評価

### 1. `config.py` (103行)

| 観点 | スコア |
|------|--------|
| 可読性 | 90 |
| メンテナンス性 | 80 |
| 堅牢性 | 75 |
| **総合** | **82** |

**良い点**: 明確な責務分離、直感的なAPI設計。
**問題点**:
- ファイルロックなし。並行アクセスでデータ破損の可能性
- `list_agents` / `get_agent` で `_read_system_prompt` を毎回呼ぶI/Oコスト（現時点では実害なし）

---

### 2. `chat.py` (66行)

| 観点 | スコア |
|------|--------|
| 可読性 | 80 |
| メンテナンス性 | 50 |
| 堅牢性 | 65 |
| **総合** | **65** |

**問題点**:
- **未使用クラス**: `ChatManager` はどのルートからも参照されていない（`routes/chat.py` が直接 `SessionReader` / `CLIBridge` を使用）
- **未使用import**: `AsyncGenerator` が使われていない
- `summarize_session` が `self._bridge.summarize()` を呼ぶが、`CLIBridge` にそのメソッドは存在しない → デッドコード
- メタデータ書き込みパターンが `routes/chat.py` と重複

---

### 3. `task_manager.py` (229行)

| 観点 | スコア |
|------|--------|
| 可読性 | 85 |
| メンテナンス性 | 80 |
| 堅牢性 | 75 |
| **総合** | **80** |

**良い点**: 公開API/内部ヘルパーの明確な分離、フロントマターパーサーの実装。
**問題点**:
- **未使用import**: `random`, `string` が使われていない
- `_parse_frontmatter` は簡易実装で、YAML仕様の一部しかサポートしない（現用途では十分）
- `approve` 後に `get_task` を二重呼び出ししている（パフォーマンス上は無害）

---

### 4. `scheduler.py` (220行)

| 観点 | スコア |
|------|--------|
| 可読性 | 85 |
| メンテナンス性 | 80 |
| 堅牢性 | 80 |
| **総合** | **82** |

**良い点**: ライフサイクル管理が堅実、ロジック表ベースの状態管理、適切なログ出力。
**問題点**:
- `_select_task` の返り値型が `tuple | None` で要素の意味が不明確（名前付きタプルかdataclassが望ましい）
- `_run_session` の引数 `agent` の型注釈が欠落

---

### 5. `session_reader.py` (185行)

| 観点 | スコア |
|------|--------|
| 可読性 | 85 |
| メンテナンス性 | 85 |
| 堅牢性 | 75 |
| **総合** | **82** |

**良い点**: アダプターパターンによる抽象化、コンパクションサマリーのスキップ処理。
**問題点**:
- `_extract_messages` 内でアシスタントメッセージのテキストが最後の `text` ブロックで上書きされる（複数textブロック時に先頭が消失）
- メタデータJSONのパースエラーが無処理

---

### 6. `task_context.py` (21行)

| 観点 | スコア |
|------|--------|
| 可読性 | 95 |
| メンテナンス性 | 90 |
| 堅牢性 | 70 |
| **総合** | **85** |

**良い点**: 最小限のコードで明確な責務。
**問題点**:
- テンプレートファイルが存在しない場合のエラーハンドリングなし（`FileNotFoundError` が未処理で伝播）
- `mode` パラメータの値検証なし

---

### 7. `cli_bridge.py` (683行)

| 観点 | スコア |
|------|--------|
| 可読性 | 60 |
| メンテナンス性 | 50 |
| 堅牢性 | 70 |
| **総合** | **60** |

**最大の問題ファイル。** プロジェクト全体のコード品質を引き下げている。

**問題点**:
- **責務過多**: プロセスプール管理、推論状態判定、PIDファイル管理、孤児プロセスクリーンアップ、デバッグ情報生成、CLIランチャーが1ファイルに凝集
- **ロジック重複**: `inferring_session_ids` と `process_debug_info` が推論判定ロジックをそれぞれ独自に実装（約70行の重複相当）
- **デバッグprint散在**: `print(f"[DEBUG] ...")`  が本番コードに残存（loggerと混在）
- `_pool_key` で `id(asyncio.current_task())` を使用 → タスク再利用時にキー衝突の可能性
- `_build_command` で `-p ""` を空文字列で渡している意図が不明確

---

### 8. `app.py` (92行)

| 観点 | スコア |
|------|--------|
| 可読性 | 90 |
| メンテナンス性 | 85 |
| 堅牢性 | 80 |
| **総合** | **85** |

**良い点**: ファクトリパターン、DI対応、lifespanでの適切なリソース管理。
**問題点**:
- `print` でのログ出力（loggerに統一すべき）

---

### 9. `routes/deps.py` (28行)

| 観点 | スコア |
|------|--------|
| 可読性 | 95 |
| メンテナンス性 | 90 |
| 堅牢性 | 85 |
| **総合** | **90** |

**良い点**: シンプルで明確。
**問題点**:
- `get_scheduler` のみ返り値型注釈が欠落し、ローカルimportを使用（他の関数と不整合）

---

### 10. `routes/agents.py` (81行)

| 観点 | スコア |
|------|--------|
| 可読性 | 85 |
| メンテナンス性 | 75 |
| 堅牢性 | 80 |
| **総合** | **80** |

**良い点**: 各エンドポイントが簡潔。
**問題点**:
- レスポンス用dict構築が `list_agents` / `get_agent` / `update_agent` で重複（レスポンスモデルを定義すべき）

---

### 11. `routes/chat.py` (245行)

| 観点 | スコア |
|------|--------|
| 可読性 | 65 |
| メンテナンス性 | 50 |
| 堅牢性 | 70 |
| **総合** | **62** |

**`cli_bridge.py` に次ぐ問題ファイル。**

**問題点**:
- **メタデータ更新パターンの重複**: `update_session_title`, `update_session_model_tier`, `hide_session` が全く同じ read-modify-write パターンを個別に実装（約15行 × 3箇所）
- **ローカルimport**: `from pathlib import Path` が3箇所の関数内に散在、`import json` が `hide_session` 内にも存在（ファイル先頭で既にimport済み）
- `event_stream` 内のtool_use記述で `chr(92)` を使ったバックスラッシュ分割が不可解

---

### 12. `routes/tasks.py` (110行)

| 観点 | スコア |
|------|--------|
| 可読性 | 85 |
| メンテナンス性 | 80 |
| 堅牢性 | 80 |
| **総合** | **82** |

**良い点**: `_tm` ヘルパーによるボイラープレート削減。
**問題点**:
- `prefix` をルーターレベルで設定せず、各エンドポイントにフルパスを記述している（他のルーターとの不整合）

---

### 13. `routes/scheduler.py` (20行)

| 観点 | スコア |
|------|--------|
| 可読性 | 95 |
| メンテナンス性 | 95 |
| 堅牢性 | 85 |
| **総合** | **92** |

特に問題なし。

---

## 総合サマリー

| コンポーネント | 可読性 | メンテナンス性 | 堅牢性 | 総合 |
|-------------|--------|-------------|--------|------|
| `config.py` | 90 | 80 | 75 | **82** |
| `chat.py` | 80 | 50 | 65 | **65** |
| `task_manager.py` | 85 | 80 | 75 | **80** |
| `scheduler.py` | 85 | 80 | 80 | **82** |
| `session_reader.py` | 85 | 85 | 75 | **82** |
| `task_context.py` | 95 | 90 | 70 | **85** |
| `cli_bridge.py` | 60 | 50 | 70 | **60** |
| `app.py` | 90 | 85 | 80 | **85** |
| `routes/deps.py` | 95 | 90 | 85 | **90** |
| `routes/agents.py` | 85 | 75 | 80 | **80** |
| `routes/chat.py` | 65 | 50 | 70 | **62** |
| `routes/tasks.py` | 85 | 80 | 80 | **82** |
| `routes/scheduler.py` | 95 | 95 | 85 | **92** |
| **プロジェクト平均** | **84** | **76** | **76** | **79** |

---

## 優先対応事項（今回のリファクタリングスコープ）

1. **未使用importの削除** — `chat.py`: `AsyncGenerator`, `task_manager.py`: `random`, `string`
2. **未使用クラスの削除** — `ChatManager`（`chat.py` 全体）
3. **`routes/chat.py` メタデータ更新の共通化** — 3箇所の重複パターンをヘルパー関数に抽出
4. **`routes/chat.py` ローカルimportの整理** — `Path`, `json` をファイル先頭に統合
5. **命名改善** — `_select_task` の返り値型明確化、`_tm` の命名改善検討

## スコープ外（将来課題）

- `cli_bridge.py` の責務分割（推論判定ロジックの抽出、PIDファイル管理の分離）
- `inferring_session_ids` と `process_debug_info` の推論判定ロジック統合
- デバッグprintのlogger統一
- レスポンスモデルの導入
